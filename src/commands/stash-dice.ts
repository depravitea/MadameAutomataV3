import { SlashCommandBuilder } from 'discord.js'; import { spendGems, addGems } from '../lib/economy.js'; export const data=new SlashCommandBuilder().setName('stash-dice').setDescription('Roll dice to win/lose gems').addIntegerOption(o=>o.setName('wager').setDescription('Wager').setRequired(true)); export async function execute(i){ const w=i.options.getInteger('wager',true); const ok=await spendGems(i.guildId!,i.user.id,w); if(!ok) return i.reply({content:'Not enough gems.',ephemeral:true}); const player=Math.floor(Math.random()*6)+1; const house=Math.floor(Math.random()*6)+1; if(player>house){ await addGems(i.guildId!,i.user.id,w*2,'dice'); await i.reply({ content:`You roll ${player} vs ${house}. You win **${w}** 💎.`}); } else { await i.reply({ content:`You roll ${player} vs ${house}. House keeps your **${w}** 💎.`}); } }
