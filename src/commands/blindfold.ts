import { SlashCommandBuilder, PermissionFlagsBits } from 'discord.js'; export const data=new SlashCommandBuilder().setName('blindfold').setDescription('Toggle the Blindfold role').addUserOption(o=>o.setName('user').setDescription('Target').setRequired(true)).addBooleanOption(o=>o.setName('on').setDescription('On/Off').setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.ManageRoles); export async function execute(i){ const user=i.options.getUser('user',true); const on=i.options.getBoolean('on',true); const m=await i.guild.members.fetch(user.id); const role=i.guild.roles.cache.find(r=>r.name.toLowerCase().includes('blindfold')); if(!role) return i.reply({content:'Create a Blindfold role or set it in /config.',ephemeral:true}); await m.roles[on?'add':'remove'](role); await i.reply({ content:`${on?'❥':'☑'} Blindfold ${on?'applied to':'removed from'} ${user}.`}); }
