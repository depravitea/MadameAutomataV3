import { SlashCommandBuilder, PermissionFlagsBits } from 'discord.js'; import { spendGems } from '../lib/economy.js'; export const data=new SlashCommandBuilder().setName('punish-bondage').setDescription('Apply a restrictive role').addUserOption(o=>o.setName('target').setDescription('User').setRequired(true)).addRoleOption(o=>o.setName('role').setDescription('Restrictive role').setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.ManageRoles); export async function execute(i){ const t=i.options.getUser('target',true); const role=i.options.getRole('role',true); const ok=await spendGems(i.guildId!,i.user.id,5); if(!ok) return i.reply({content:'Not enough gems.',ephemeral:true}); const m=await i.guild.members.fetch(t.id); await m.roles.add(role.id); await i.reply({ content:`${t} is bound by ${role}.`}); }
