import { SlashCommandBuilder, PermissionFlagsBits } from 'discord.js'; export const data=new SlashCommandBuilder().setName('mark').setDescription('Apply a mark to a user nickname').addUserOption(o=>o.setName('user').setDescription('Target').setRequired(true)).addStringOption(o=>o.setName('prefix').setDescription('Prefix')).addStringOption(o=>o.setName('suffix').setDescription('Suffix')).setDefaultMemberPermissions(PermissionFlagsBits.ManageNicknames); export async function execute(i){ const u=i.options.getUser('user',true); const m=await i.guild.members.fetch(u.id); const prefix=i.options.getString('prefix')||'❦'; const suffix=i.options.getString('suffix')||''; const base=m.nickname||u.username; await m.setNickname(`${prefix} ${base} ${suffix}`.trim()); await i.reply({ content:`Marked ${u}.`}); }
