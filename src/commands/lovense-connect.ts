import { SlashCommandBuilder } from 'discord.js'; import { isEnabled } from '../lib/flags.js'; export const data=new SlashCommandBuilder().setName('lovense-connect').setDescription('Connect to a Lovense session (placeholder)'); export async function execute(i){ if(!await isEnabled(i.guildId!,'lovense')) return i.reply({ content:'Lovense features are disabled.',ephemeral:true}); await i.reply({ content:'Provide your Lovense link via DM to a staff-approved channel. (Integration placeholder)'}); }
