import { SlashCommandBuilder } from 'discord.js'; import { prisma } from '../lib/db.js'; import { ensureProfile } from '../lib/economy.js'; export const data=new SlashCommandBuilder().setName('dynamic-simp').setDescription('Simp for someone publicly').addUserOption(o=>o.setName('target').setDescription('Who to simp for').setRequired(true)); export async function execute(i){ const t=i.options.getUser('target',true); const p=await ensureProfile(i.guildId!,t.id); await prisma.profile.update({ where:{id:p.id}, data:{ simpCount:{ increment:1 } } }); await i.reply({ content:`${i.user} simps for ${t}. A bouquet of crimson admiration.`}); }
