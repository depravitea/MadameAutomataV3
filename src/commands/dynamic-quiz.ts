import { SlashCommandBuilder } from 'discord.js'; import { prisma } from '../lib/db.js'; export const data=new SlashCommandBuilder().setName('dynamic-quiz').setDescription('Take a personality/kink quiz and store results'); export async function execute(i){ const sample={ alignment:'Service-Oriented', intensity:'Medium', limits:['stingy pain','public'], prefs:['praise','protocol'] }; const r=await prisma.quizresult.create({ data:{ guildId:i.guildId!, userId:i.user.id, result: JSON.stringify(sample) } }); await i.reply({ content:`Quiz complete. Result saved (${r.id}).`}); }
