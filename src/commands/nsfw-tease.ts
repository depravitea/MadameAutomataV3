import { SlashCommandBuilder } from 'discord.js'; import { isEnabled } from '../lib/flags.js'; export const data=new SlashCommandBuilder().setName('nsfw-tease').setDescription('NSFW tease (feature-gated)'); export async function execute(i){ if(!await isEnabled(i.guildId!,'nsfw')) return i.reply({ content:'NSFW features are disabled.',ephemeral:true}); await i.reply({ content:'A shadowed whisper passes… (NSFW action executed; details omitted per ToS).' }); }
