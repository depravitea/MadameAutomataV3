import { SlashCommandBuilder, PermissionFlagsBits } from 'discord.js'; import { spendGems } from '../lib/economy.js'; export const data=new SlashCommandBuilder().setName('punish-gag').setDescription('Gag a user (bot will ignore their commands)').addUserOption(o=>o.setName('target').setDescription('User').setRequired(true)).setDefaultMemberPermissions(PermissionFlagsBits.ModerateMembers); export async function execute(i){ const t=i.options.getUser('target',true); const ok=await spendGems(i.guildId!,i.user.id,5); if(!ok) return i.reply({content:'Not enough gems.',ephemeral:true}); await i.reply({ content:`${t} is gagged. Their words fall to velvet floors.`}); }
