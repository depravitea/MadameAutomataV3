import { SlashCommandBuilder, PermissionFlagsBits } from 'discord.js'; export const data=new SlashCommandBuilder().setName('ticket').setDescription('Set up a simple ticket channel trigger').setDefaultMemberPermissions(PermissionFlagsBits.ManageChannels); export async function execute(i){ await i.reply({ content:'Open a ticket by messaging staff. (Stub panel here.)', ephemeral:true});}
