import { SlashCommandBuilder, PermissionFlagsBits } from 'discord.js'; export const data=new SlashCommandBuilder().setName('reward').setDescription('Reward a submissive').addUserOption(o=>o.setName('submissive').setDescription('User').setRequired(true)).addStringOption(o=>o.setName('reason').setDescription('Reason')); export async function execute(i){ const sub=i.options.getUser('submissive',true); const reason=i.options.getString('reason')||'good behavior'; await i.reply({ content:`A crimson ribbon for ${sub}: **${reason}**.`}); }
