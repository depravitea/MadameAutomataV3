import { SlashCommandBuilder } from 'discord.js'; import { addGems } from '../lib/economy.js'; export const data=new SlashCommandBuilder().setName('stash-work').setDescription('Work to earn gems (cooldown not enforced in demo)'); export async function execute(i){ const earned=Math.floor(Math.random()*5)+1; await addGems(i.guildId!,i.user.id,earned,'work'); await i.reply({ content:`${i.user} labors dutifully and earns **${earned}** 💎.`}); }
